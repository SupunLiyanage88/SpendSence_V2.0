<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SpendSence" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#181818</item>
        <item name="colorPrimaryDark">#181818</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="android:actionBarStyle">@style/ActionBarBlack</item>
        <item name="android:windowTranslucentNavigation">true</item> <!-- Enable translucent navigation -->
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:navigationBarColor">@android:color/transparent</item> <!-- Make navigation bar transparent -->
    </style>

    <style name="Theme.SpendSence" parent="Base.Theme.SpendSence" />

    <!-- Action Bar Style -->
    <style name="ActionBarBlack" parent="Widget.AppCompat.ActionBar.Solid">
        <item name="android:background">#181818</item>
        <!-- Removed text color setting -->
    </style>

    <!-- Action Bar Title Text Style -->
    <style name="ActionBarTitleText" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <!-- Removed text color setting -->
    </style>
</resources>
